//* packages import
import React from 'react';
import {OtpInput, OtpInputProps} from 'react-native-otp-entry';

const OTPInput = (props: OtpInputProps): React.JSX.Element => {
  return (
    <OtpInput
      {...props}
    />
  );
};

export default OTPInput;
