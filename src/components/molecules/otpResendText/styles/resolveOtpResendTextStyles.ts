import type {ThemeTokens} from '@theme/types';

export const resolveOtpResendTextStyles = (tokens: ThemeTokens) => ({
    timerText: {
      marginTop: 20,
      color: tokens.colors.textMuted,
    },
  });
