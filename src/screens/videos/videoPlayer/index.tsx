import React from 'react';
import {Text, View} from 'react-native';

const VideoPlayer = (props: any): React.JSX.Element => {
  return (
    <View>
      <Text>VideoPlayer</Text>
    </View>
  );
};

export default VideoPlayer;
