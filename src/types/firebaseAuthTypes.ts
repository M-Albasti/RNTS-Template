/** Modular Firebase Auth types (React Native Firebase v26+). */
export type {
  AdditionalUserInfo,
  ConfirmationResult,
  NativeFirebaseAuthError,
  User,
  UserCredential,
  UserMetadata,
} from '@react-native-firebase/auth';
