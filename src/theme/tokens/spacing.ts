/** Consistent spacing scale (4px base). Use these instead of magic numbers in StyleSheet. */
export const spacing = {
  none: 0,
  xxs: 2,
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
  xxxl: 48,
} as const;

export type SpacingToken = typeof spacing;
