diff --git a/node_modules/react-native-nitro-sound/ios/Sound.swift b/node_modules/react-native-nitro-sound/ios/Sound.swift
index c680b8d..0479c01 100644
--- a/node_modules/react-native-nitro-sound/ios/Sound.swift
+++ b/node_modules/react-native-nitro-sound/ios/Sound.swift
@@ -2,6 +2,15 @@ import Foundation
 import AVFoundation
 import NitroModules
 
+// Xcode 16 SDK exposes `.allowBluetooth`; Xcode 26 renamed it to `.allowBluetoothHFP`.
+private let nitroSoundBluetoothHFPOption: AVAudioSession.CategoryOptions = {
+    #if compiler(>=6.2)
+    return .allowBluetoothHFP
+    #else
+    return .allowBluetooth
+    #endif
+}()
+
 final class HybridSound: HybridSoundSpec_base, HybridSoundSpec_protocol {
     // MARK: - Audio Quality Presets (matching Android implementation)
     private struct QualitySettings {
@@ -65,7 +74,7 @@ final class HybridSound: HybridSoundSpec_base, HybridSoundSpec_protocol {
 
                 try self.recordingSession?.setCategory(.playAndRecord,
                                                      mode: sessionMode,
-                                                     options: [.defaultToSpeaker, .allowBluetoothHFP])
+                                                     options: [.defaultToSpeaker, nitroSoundBluetoothHFPOption])
                 try self.recordingSession?.setActive(true)
 
                 print("🎙️ Audio session set up successfully")
@@ -187,7 +196,7 @@ final class HybridSound: HybridSoundSpec_base, HybridSoundSpec_protocol {
                                 do {
                                     // Reuse existing session instance (singleton)
                                     let sessionMode = audioSets?.AVModeIOS.map(self.getAudioSessionMode) ?? .default
-                                    try audioSession.setCategory(.playAndRecord, mode: sessionMode, options: [.defaultToSpeaker, .allowBluetoothHFP, .mixWithOthers])
+                                    try audioSession.setCategory(.playAndRecord, mode: sessionMode, options: [.defaultToSpeaker, nitroSoundBluetoothHFPOption, .mixWithOthers])
                                     try audioSession.setActive(true, options: [])
                                     print("🎙️ ✅ Audio session recovered successfully")
                                 } catch {
@@ -201,7 +210,7 @@ final class HybridSound: HybridSoundSpec_base, HybridSoundSpec_protocol {
                                 do {
                                     let sessionMode = audioSets?.AVModeIOS.map(self.getAudioSessionMode) ?? .default
                                     try audioSession.setActive(false, options: .notifyOthersOnDeactivation)
-                                    try audioSession.setCategory(.playAndRecord, mode: sessionMode, options: [.defaultToSpeaker, .allowBluetoothHFP])
+                                    try audioSession.setCategory(.playAndRecord, mode: sessionMode, options: [.defaultToSpeaker, nitroSoundBluetoothHFPOption])
                                     try audioSession.setActive(true)
                                     print("🎙️ ✅ Forced immediate session takeover")
                                 } catch {
@@ -248,7 +257,7 @@ final class HybridSound: HybridSoundSpec_base, HybridSoundSpec_protocol {
                                     let sessionMode = audioSets?.AVModeIOS.map(self.getAudioSessionMode) ?? .default
                                     try audioSession.setCategory(.playAndRecord,
                                                                mode: sessionMode,
-                                                               options: [.defaultToSpeaker, .allowBluetoothHFP])
+                                                               options: [.defaultToSpeaker, nitroSoundBluetoothHFPOption])
                                     try audioSession.setActive(true)
                                     print("🎙️ Audio session fully reset for retry")
                                 } catch {
@@ -309,7 +318,7 @@ final class HybridSound: HybridSoundSpec_base, HybridSoundSpec_protocol {
                                         let sessionMode = audioSets?.AVModeIOS.map(self.getAudioSessionMode) ?? .default
                                         try audioSession.setCategory(.playAndRecord,
                                                                    mode: sessionMode,
-                                                                   options: [.defaultToSpeaker, .allowBluetoothHFP, .mixWithOthers])
+                                                                   options: [.defaultToSpeaker, nitroSoundBluetoothHFPOption, .mixWithOthers])
                                         try audioSession.setActive(true, options: [])
                                     } catch {
                                         print("🎙️ Warning: Could not set mixing category: \(error)")
@@ -321,7 +330,7 @@ final class HybridSound: HybridSoundSpec_base, HybridSoundSpec_protocol {
                                             let sessionMode = audioSets?.AVModeIOS.map(self.getAudioSessionMode) ?? .default
                                             try audioSession.setCategory(.playAndRecord,
                                                                        mode: sessionMode,
-                                                                       options: [.defaultToSpeaker, .allowBluetoothHFP])
+                                                                       options: [.defaultToSpeaker, nitroSoundBluetoothHFPOption])
                                             try audioSession.setActive(true)
                                             print("🎙️ Audio session corrected and exclusively activated")
                                         } catch let error as NSError {
